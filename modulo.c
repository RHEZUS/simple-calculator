#include "main.h"

/**
 * modulo - makes the modulo of two numbers
 * @a: the first number
 * @b: the second number
 * Return: the the rest of the division of a by b
 */

int modulo(int a, int b);

int modulo(int a, int b)
{       
	return (a % b);
}
