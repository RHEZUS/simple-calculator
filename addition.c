#include "main.h"

/**
 * addition - makes the addition of two numbers
 * @a: the first number
 * @b: the second number
 * Return: the summ of the two numbers
 */


double addition(double a, double b)
{
	return (a + b);
}
